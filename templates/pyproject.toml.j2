[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "Metadata curation project for {{ pi_name }}"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1",
    "requests>=2.31",
    "python-dotenv>=1.0",
    "smbprotocol>=1.10",
]
